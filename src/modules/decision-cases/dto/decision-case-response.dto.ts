export type DecisionCaseResponseDto = {
  id: string;
  problemId: string;
  title: string;
  stage: string;
  status: string;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
};
